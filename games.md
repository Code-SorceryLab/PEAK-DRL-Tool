| Game                                                 | Random / Procedural Element(s)                                                                                                                                                                                 | Why it’s easy to refactor                                                                                                                                                   | LoC (rough) / Licence                          |
| ---------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------- |
| **Goodoo** – `clau555/goodoo`                        | *Cellular-automata cave generator* builds a fresh vertical level every run; lava-rise timer + random wall obstacles                                                                                            | Single entry-point (`goodoo.py`) calls functions in `src/`, so you can intercept `main()` and expose `.reset(seed)` / `.step(action)` without touching the render loop      | 3 k LoC, GPL-3; Pygame-only deps ([GitHub][1]) |
| **Jumpy** – `russs123/Jumpy`                         | Infinite “Doodle-Jump” style: platforms are spawned with random **x-pos, width & vertical gap** each frame (`random.randint…`, `MAX_PLATFORMS = 10`) ([GitHub][2])                                             | Whole game lives in one file (`jumpy_tut8.py`, \~220 LoC).  Sprite groups already separated → wrap update/draw per tick or run head-less by stubbing `pygame.display.*`     | \~220 LoC, MIT                                 |
| **Doodle-Jump-PyGame** – `faridk/doodle-jump-pygame` | Uniform-random platform list per frame; project was **written for OpenAI Gym integration**, so a minimal `.step()` loop is already present in `game.py` and just needs a proper return signature ([GitHub][3]) | Only three files (assets, `game.py`, `README.md`).  Clear `Game` class with `update()` that you can hijack for agent control                                                | 180 LoC, MIT                                   |
| **Jetblade** (classic but still useful)              | Roguelike-style **procedurally generated Metroid-vania maps** at every reset ([Pygame][4])                                                                                                                     | Core world-generation is in a separate module; you can short-circuit the render thread and sample the tilemap directly for state vectors                                    | \~4 k LoC, GPL-2                               |                 |

[1]: https://github.com/clau555/goodoo "GitHub - clau555/goodoo: Experimental platformer video game made in Python, based on procedural generation and a physics mechanics."
[2]: https://raw.githubusercontent.com/russs123/Jumpy/main/jumpy_tut8.py "raw.githubusercontent.com"
[3]: https://github.com/faridk/doodle-jump-pygame "GitHub - faridk/doodle-jump-pygame: Doodle Jump clone with the main character inspired by Geometry Dash's protagonist"
[4]: https://www.pygame.org/project/1225 "Jetblade"
